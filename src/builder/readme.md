O Builder permite construir um objeto complexo passo a passo, isolando a construção da representação final.

Como está no código:
O CharacterBuilder é responsável por criar personagens a partir dos componentes (raça e classe) usando o Abstract Factory para montar o personagem completo, facilitando a criação com diferentes combinações.

Arquivos principais:

    characterBuilder.py